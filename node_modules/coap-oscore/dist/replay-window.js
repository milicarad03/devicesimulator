"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ReplayWindow = void 0;
const constants_1 = require("./constants");
class ReplayWindow {
    highestSsn = -1n;
    window = 0n; // bitmask — bit i represents (highestSsn - i)
    isValid(ssn) {
        if (ssn > this.highestSsn) {
            return true;
        }
        const diff = this.highestSsn - ssn;
        if (diff >= BigInt(constants_1.REPLAY_WINDOW_SIZE)) {
            return false; // too old
        }
        // Check if bit at position `diff` is already set
        if ((this.window >> diff) & 1n) {
            return false; // duplicate
        }
        return true;
    }
    update(ssn) {
        if (ssn > this.highestSsn) {
            const shift = ssn - this.highestSsn;
            if (shift < BigInt(constants_1.REPLAY_WINDOW_SIZE)) {
                this.window = (this.window << shift) | 1n;
            }
            else {
                this.window = 1n;
            }
            this.highestSsn = ssn;
        }
        else {
            const diff = this.highestSsn - ssn;
            this.window |= (1n << diff);
        }
    }
    reinit(currentSsn) {
        this.highestSsn = currentSsn;
        // Fill the entire window so all SSNs <= currentSsn are marked as seen.
        // This prevents replays of messages from before the restore point.
        if (currentSsn >= 0n) {
            this.window = (1n << BigInt(constants_1.REPLAY_WINDOW_SIZE)) - 1n;
        }
        else {
            this.window = 0n;
        }
    }
}
exports.ReplayWindow = ReplayWindow;
