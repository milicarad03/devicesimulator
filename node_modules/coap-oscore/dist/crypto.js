"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.defaultAeadProvider = void 0;
exports.deriveKeyOrIV = deriveKeyOrIV;
exports.createAAD = createAAD;
exports.createEncStructure = createEncStructure;
exports.aesCcmEncrypt = aesCcmEncrypt;
exports.aesCcmDecrypt = aesCcmDecrypt;
const node_crypto_1 = require("node:crypto");
const cbor_1 = require("cbor");
const constants_1 = require("./constants");
function deriveKeyOrIV(masterSecret, masterSalt, id, idContext, type, length) {
    // HKDF info structure per RFC 8613 Section 3.1:
    // info = [ id: bstr, id_context: bstr / null, alg_aead: int, type: tstr, L: uint ]
    const info = (0, cbor_1.encode)([
        id,
        idContext && idContext.length > 0 ? idContext : null,
        constants_1.AEAD_ALG_ID,
        type,
        length,
    ]);
    // HKDF-SHA-256: extract with masterSalt, expand with info
    const salt = masterSalt.length > 0 ? masterSalt : Buffer.alloc(0);
    const derived = (0, node_crypto_1.hkdfSync)('sha256', masterSecret, salt, info, length);
    return Buffer.from(derived);
}
function createAAD(requestKid, requestPiv) {
    // External AAD per RFC 8613 Section 5.4:
    // aad_array = [ oscore_version: uint, algorithms: [alg_aead: int], request_kid: bstr, request_piv: bstr, options: bstr ]
    return Buffer.from((0, cbor_1.encode)([
        1, // oscore_version
        [constants_1.AEAD_ALG_ID], // algorithms array
        requestKid, // request_kid
        requestPiv, // request_piv
        Buffer.alloc(0), // options (empty bstr)
    ]));
}
function createEncStructure(aad) {
    // COSE Encrypt0 structure per RFC 9052 Section 5.3:
    // Enc_structure = ["Encrypt0", h'', external_aad]
    return Buffer.from((0, cbor_1.encode)([
        'Encrypt0',
        Buffer.alloc(0), // protected header (empty for OSCORE)
        aad,
    ]));
}
function aesCcmEncrypt(key, nonce, plaintext, aad) {
    const cipher = (0, node_crypto_1.createCipheriv)('aes-128-ccm', key, nonce, { authTagLength: constants_1.AUTH_TAG_LEN });
    cipher.setAAD(aad, { plaintextLength: plaintext.length });
    const encrypted = cipher.update(plaintext);
    cipher.final();
    const tag = cipher.getAuthTag();
    return Buffer.concat([encrypted, tag]);
}
function aesCcmDecrypt(key, nonce, ciphertextWithTag, aad) {
    if (ciphertextWithTag.length < constants_1.AUTH_TAG_LEN) {
        throw new Error('Ciphertext too short');
    }
    const ciphertext = ciphertextWithTag.subarray(0, ciphertextWithTag.length - constants_1.AUTH_TAG_LEN);
    const tag = ciphertextWithTag.subarray(ciphertextWithTag.length - constants_1.AUTH_TAG_LEN);
    const decipher = (0, node_crypto_1.createDecipheriv)('aes-128-ccm', key, nonce, { authTagLength: constants_1.AUTH_TAG_LEN });
    decipher.setAuthTag(tag);
    decipher.setAAD(aad, { plaintextLength: ciphertext.length });
    const decrypted = decipher.update(ciphertext);
    decipher.final(); // throws on auth failure
    return Buffer.from(decrypted);
}
exports.defaultAeadProvider = {
    encrypt: aesCcmEncrypt,
    decrypt: aesCcmDecrypt,
};
